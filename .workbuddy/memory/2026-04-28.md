# 2026-04-28 · Daily

## taskId 后缀（`EV-0003-cWQMJx` 里的 `cWQMJx`）的必要性辩证

### 触发
用户在会话里看到多次出现的 `EV-0003-cWQMJx`、`EV-0003-jPHHCU` 这种带 6 位随机后缀的 taskId，疑惑后缀到底是干啥的、是否多余。

### 结论：必要，不能删

### 三大现实理由（按重要性）
1. **reconcile 身份锚**（硬依赖）：`codeRegistry.ts::buildTaskIdToQueryIdMap` 同时建 `byTaskId` 和 `bySuffix` 双索引。`queryCode`（EV-0003）会被启动期 reconcile 归一化重命名，但 suffix 永不变，是 inbox/outbox/query 三方唯一稳定的跨文件引用键。
2. **并发防撞**：seed / migrate 脚本批量跑时，同秒同 code 生成有概率撞车，suffix（nano6 = 6 位字母数字，≈57 bit 熵）是"几乎不可能碰撞"的唯一保证。
3. **URL capability 弱防护**：taskId 是所有 `:taskId` 端点的 URL slug，6 位随机后缀让 URL 自带 token 性质 —— 不知道完整 taskId 就访问不到对应文件。

### 代价（诚实列一列）
- 日志 / URL / filename 可读性下降（7 字符视觉噪音）。
- 用户在 UI 上看到会产生"这是什么"的认知成本（本次对话就是例证）。
- **但这些都是展示层问题，不是身份层问题。**

### 后续可做（非本次范围）
- 日志/UI 里给 taskId 加 tooltip 或分段展示，明示"EV-0003 = 业务号 / -cWQMJx = 不可变锚"，降低认知门槛。
- 如果要进一步优化，可以在人可读场景同时展示 `queryCode` + `taskId` 两段。

### 涉及文件（只读，本次无代码改动）
- `src/lib/contract.ts:572-587`（`nano6` + `makeTaskId`）
- `vite-plugins/codeRegistry.ts:351-395`（`parsePrefixCode` + `buildTaskIdToQueryIdMap` + `extractSuffix`）
- `vite-plugins/codeRegistry.ts:408-426`（`renamePrefix`，reconcile 重命名时保留 suffix 的关键逻辑）
- `vite-plugins/bus/helpers.ts:97-101`（`parseQueryCode` 从 taskId 反解 queryCode）

### 沉淀
已更新 `MEMORY.md` → 新增"taskId 双段结构"条目，写清楚设计出处和不可删原因，避免后续再次踩同款认知疑惑。

---

## 胶囊"未参评"徽标升级为"待评测"：替换正文后也能提醒

### 触发
用户观察到：先前给 ReportPage cohort 胶囊加的"未参评"徽标只能抓到「新增产品从未召唤」这一种场景；**走「编辑对比源 → 替换正文」后没再召唤评测**的场景完全不提醒（因为这份 submission.id 早就在 inbox 里，`inboxHistoryMap.has()` 仍为 true）。用户直觉判断："其实应该对比 最新评测时间 vs 最新对比源变动时间（只看正文变动，不算元数据改动）"。

### 辩证结论
用户直觉正确，且比旧实现语义更干净。旧判据是"submission 是否进过 inbox"（布尔），新判据是"最新一次正文变动 vs 最新一次评测"（时间比较）。新模型天然契合契约：contract 保证改元数据走 `updateSubmission` 只动 localStorage 不追加 `reportVersions`；只有替换正文走 PATCH inbox 才会 push 一条新 version，所以用 `reportVersions[last].submittedAt` 做"正文变动锚"干净无歧义。

### 实现（`src/pages/ReportPage.tsx`，~70 行增量）
- 新增派生 `latestEvalMtime = max(outboxTasks.latestMtime)` 和 `pendingSubIds: Set<string>`，对每个 submission 取 `inbox.reportVersions[last].submittedAt`（回退链：`submittedAt → replacedAt → producedAt → sub.createdAt`），与 `latestEvalMtime` 比大小。
- Hero 胶囊 `showPendingBadge` 从 `!hasInbox` 换成 `pendingSubIds.has(sub.id)`；徽标文案 `未参评 → 待评测`；tooltip 区分两种场景（全新未召唤 / 正文已更新待重评）。
- 图例出现条件从 `cohort.some(没 inbox)` 换成 `pendingSubIds.size > 0`；文案改为"灰点 = 对比源在最新评测之后有更新，建议重跑评测"。
- **「召唤评测」按钮右上角加小红 badge**：`pendingSubIds.size > 0` 时出现数字徽章（`bg-clay text-white` + `ring-2 ring-white`，18px 圆），tooltip 提示"有 N 个对比源在最新评测之后发生了变动，建议重跑一次评测"。
- **零改动**：store / types / contract / lint / inbox/outbox 写入链路完全不动，纯前端派生。

### 踩坑：React Hooks 顺序
首次落地把两个 `useMemo` 放在了 `if (!q) return` **之后**，触发 `react-hooks/rules-of-hooks` 报错。必须移到所有 early return 之前。已在代码注释里显式标注。

### 回归
- `npm run lint` ✅
- `npx tsc -p tsconfig.app.json --noEmit` ✅
- `npm test` ✅（126 用例）

### 边界复盘
- `IS_READONLY` 下 `pendingSubIds` 被强制清空 → 徽标 + badge 整体不露（保持公开页面干净）。
- `latestEvalMtime === 0`（从未评测）时所有 submission 都 pending → 行为正确（"完全没评过 = 全部待评"）。
- 时钟回跳 / mtime 异常等极端场景最坏是"多提醒一次"，不会漏打；可接受。

---

## 按钮 badge 二次迭代：数字 → 脉冲小圆点

### 触发
用户反馈："其实没必要给数字，只需要一个示意，提醒说'需要再跑一次新的评测'即可。"——数字是过载信息，用户只需要"有/无"这个信号量。

### 设计选型辩证
列了 4 个方向：① 静态小圆点（太静）；② **脉冲小圆点（animate-ping 光晕）**；③ 小闪电/刷新 icon（和 🤖 emoji 语义打架）；④ 按钮本体变色（破坏 hover 状态）。

最终选 ②。理由：
- **零信息量但有视觉权重**：不说"几个"，只说"有"，契合用户意图。
- **脉冲呼吸**是 Linear / Notion / GitHub 等主流产品统一的"柔性提醒"语言，大众心智成本低。
- 和按钮本体 🤖 emoji 互不抢位。
- Tailwind `animate-ping` 周期 1s，足够低调不烦人。

### 实现（`src/pages/ReportPage.tsx`）
双层 span 结构（Tailwind 官方通知点标准模式）：
- **外层 `animate-ping`**：同色半透明光晕，持续脉冲，制造呼吸感。
- **内层实心圆点**：`7px × 7px` + `bg-clay` + `ring-2 ring-white` 外描边，在深色按钮背景上清晰可见。
- 位置 `-top-0.5 -right-0.5`，比之前的 18px 数字徽章更贴合按钮边缘。
- 语义通过 `aria-label` + 按钮本体 tooltip 表达"待评测数量 + 建议重跑"。

### 回归
- `npm run lint` ✅
- `npx tsc -p tsconfig.app.json --noEmit` ✅

---

## v3.4 taskId 扁平化：`taskId === queryCode`

### 触发
上一节给按钮加"待评测脉冲点"时，顺带发现一个更深的工程债：同一 query 被多次「召唤评测」会在 outbox 下生成一堆碎片化目录 `EV-0002-abc/`、`EV-0002-xyz/`...，前端 `pickLatestTaskByQueryCode` 只展示最新那个，**其余历史版本全被"埋了"**。根因是 `makeTaskId()` 每次都拼新 nano6 后缀。

### 辩证：suffix 身份锚可以让位
- 日志里记了三个保留 suffix 的理由（reconcile 身份锚 / 并发防撞 / URL 弱防护）
- 重新审视：reconcile 主键可以用 payload 里冗余的 `queryId`（UUID）承担；并发防撞场景只在 seed/migrate 批量跑，那里本来就有自己的去重；URL 弱防护是"安全感"而非真安全
- 结论：suffix 的弱防护价值 < 目录碎片化的真成本。扁平化

### 实现
- `src/lib/contract.ts::makeTaskId` → `return queryCode`（删 nano6）
- `contractVersion` 联合类型加 `"3.4"`；`DEFAULT_RUBRIC_NAMES` 同步加映射
- `vite-plugins/bus/handlers/inbox.ts::handlePostInbox`：文件已存在时**不再 409**，按 `candidateId` 合并 candidates（新的追加；已有的保留磁盘权威副本，含 reportVersions / activeReportVersion / productVersion / authorNote；顶层 query 元信息以新提交为准）
- `vite-plugins/bus/helpers.ts::parseQueryCode` 正则 `^([A-Z]+-\d+)(?:-|$)` 兼容两种形态
- `vite-plugins/codeRegistry.ts::parsePrefixCode / renamePrefix` 同时认扁平化 `EV-XXXX` 和历史 `EV-XXXX-suffix[.json]`
- `scripts/migrate-consolidate-outbox.mjs`：一次性按 mtime 合并历史目录
  - 迁移产出：6 outbox 扁平化目录 + 6 inbox 扁平化文件；旧 suffix 目录清除
  - payload 里 `taskId` 字段同步重写为扁平化形式
- `scripts/lint-outbox.mjs` 允许 `contractVersion="3.4"`

### 边界隔离再确认
vite-plugins 下的 helpers 继续遵守"禁 import src/"的硬约束，helpers.ts 里加的正则 + `inbox.ts` 里的合并逻辑全部落在 plugin 层，没有跨边界拉 src 模块。

### 回归三连（全绿）
- `npm run lint:outbox` ✅（16 outbox + 6 inbox 校验通过）
- `npm test` ✅（126 用例 pass）
- `npx tsc -p tsconfig.app.json --noEmit` ✅
- `npm run lint` ✅
- `npm run dev` 冷启 ✅（`[codeRegistry] reconcile: no-op (7 entries tracked)`）
- `npm run bake:public` ✅（6 outbox tasks → 6 latest payloads；public-bundle 6 queries / 27 submissions）

### 契约文档
- 顶部 v3.4 变更节、§2.1 taskId 约定、§5 示例、§6 历史兼容表、§6.3 渲染约定、§7 版本节 全部同步到 v3.4
- 契约语义强调：**v3.4 只是结构性工程整理，评测规则零变更**；新产物允许写 `contractVersion="3.3"` 也允许写 `"3.4"`

### 现场展示层收益
ReportPage 的 `flattenTaskVersions` + `ReportVersionPicker` 天然能展示 `EV-0002/v1/v2/.../vN`，无需前端代码改动。召唤评测按钮点了以后，会往已有目录追加 `v{N+1}`，历史不再被埋。

---

## taskId 工作复盘（2026-04-28 全量完成后）

### 心得

**一、"三个理由守住"不等于"三个理由都成立"。**
早上写 daily 时我列了 suffix 的三大必要性（reconcile 锚 / 并发防撞 / URL 弱防护），当时推论是"必要，不能删"。但下午真正评估"目录碎片化把历史版本埋掉"这个代价之后才发现：
- reconcile 锚：queryId（UUID）同样冗余在 payload 里，本来就是 reconcile 的真主键，suffix 是第二道保险
- 并发防撞：只在 seed/migrate 批量场景下发生，那里的生成器完全可以自己加去重；实际用户点"召唤评测"是手动节奏，不存在并发
- URL 弱防护：本地私有数据 + GitHub Pages 静态产物，攻击面几乎为零；"不知道完整 taskId"根本不是安全边界

三个理由不是假的，但**权重全部是"锦上添花"**，加一起也压不过"目录碎片化导致历史被埋"这个真实损伤。早上那篇 daily 有点自我说服的味道，下午推翻它是对的。

**二、契约改动的"真成本"在迁移，不在代码。**
代码改动就五六处（makeTaskId / parseQueryCode / codeRegistry / inbox POST 合并 / contractVersion 联合类型）。真正耗时间的是：
- 写 `migrate-consolidate-outbox.mjs` 时需要想清楚"两个 suffix 目录下 v1 撞了怎么办" → 按 mtime 升序排，重新分配 v1..vN
- 迁移时 payload 里 `taskId` 字段也要就地重写，否则 lint 会挂
- inbox POST 的合并语义要把"磁盘权威 vs 新提交"想清楚：reportVersions / activeReportVersion / productVersion / authorNote 走磁盘权威（保 PATCH 历史不被轧平），query 顶层元信息走新提交（允许用户改 title/description）

这些细节一个不写就翻车。真要复用经验：**结构性重构，代码只是 20%，数据迁移 + 语义兼容层是 80%。**

**三、"历史兼容"不等于"生成新旧两种"。**
v3.4 的姿势是：旧数据继续按 v1.0~v3.3 渲染（读兼容），新数据只生成扁平化形态（写单一形态）。contractVersion 联合类型里保留 "3.3" 也允许，是给"3.3 时代写完但还没来得及 bake"的产物留的缓冲，不是让我继续生成 3.3。这个区分很关键——写兼容才是债，读兼容是资产。

### 遗漏（按影响排序）

**✅ 都在读兼容边界内**，没有真漏的点，但有几个角落值得记一笔：

1. **EvaluationRunModal.tsx L43-54 注释文案过时。**
   原注释说"快速关闭会导致 nano6 重新生成新 taskId 产生孤儿"。扁平化后 taskId ≡ queryCode，重复提交走合并语义、不会生孤儿。**这条要么改文案，要么删掉整段"重复提交 bug"讨论**（现在已经不成立）。不是功能 bug，只是误导读者，优先级 P2。

2. **tests/dataSource.test.ts 的 URL 样本全部是旧 suffix 形态**（`EV-0002-N3e5zP`、`EV-0005-LlSiEs` 等）。
   测试内容本身验证 `toStaticUrl` 正则——正则对 `EV-0002` 和 `EV-0002-N3e5zP` 都成立，功能没问题。但**样本不代表现状**。后续加用例时应该至少覆盖一组扁平化形态（`/_bus/outbox/EV-0002`）。P2。

3. **scripts/replace-report.mjs 的示例命令行用的还是 `--task EV-0007-QeLgZE`**（来自历史迁移前的命令行示例）。功能不受影响（脚本吃的是字符串），但文档里的示例需要更新成 `--task EV-0007`。P3。

4. **scripts/migrate-inbox.mjs 的 CLI 帮助文案里举例 `--task EV-0005-LlSiEs`。**
   这个脚本本身已经执行完它的历史使命（v1→v2 inbox schema 迁移），长期留在 repo 是为了审计。文案更新是 P3。

5. **cleanup-orphan-inbox.mjs 对扁平化形态"天然正确"。**
   它判"inbox 有，outbox 对应 taskId 目录下没 v*.json" → 孤儿。扁平化后 `inbox/EV-0002.json` ↔ `outbox/EV-0002/`，一一对应关系更干净，不需要改。

### 可进一步优化的点

**短期（本周可做）**
- **A. 清文档债（1h）**：改 EvaluationRunModal 注释 + replace-report 示例 + dataSource.test 补一个扁平化样本。纯清洁。
- **B. lint:outbox 加个硬约束**：v3.4+ 产物的 `taskId` 必须与 outbox 目录名完全一致，且必须匹配 `^[A-Z]+-\d+$`（不带 suffix）。现在 lint 只管 `contractVersion`，不管 taskId 形态。加这一条能防止未来谁手写 payload 时意外写回老格式。P2。
- **C. codeRegistry 重命名路径**：`renamePrefix` 仍然同时处理 "EV-XXXX" 和 "EV-XXXX-suffix" 两种 key。扁平化后日常只会见到前者。保留兼容没问题，但值得加一段注释说明"后者只会在 2026-04-28 之前的历史产物里出现，新数据永远走前者"。P3。

**中期（有空再说）**
- **D. 考虑 outbox 目录锁**：扁平化之后"召唤评测"会往同一目录追加 `v{N+1}`。如果用户在 LLM 正在写 v3 时又点了一次召唤（往 inbox 提交新 candidate），理论上不冲突（inbox 合并 / outbox 按目录扫描算下一版本）。但如果哪天 LLM 并发写 v3，会撞。当前是单用户本地场景，可以忽略；但如果以后做多人协作版，需要 outbox/{taskId}/.lock 之类的轻量锁。
- **E. queryId 作为备选 URL 段**：扁平化后 `taskId === queryCode`，`queryCode` 会被 reconcile 重命名（虽然日常罕见）。一旦发生重命名，所有现存的 `outbox/EV-0002/` 都要移动到新目录。codeRegistry 已经处理了这个场景（`renamePrefix` 扫 outbox/inbox 改目录），但**URL 层仍然依赖 queryCode**。如果将来要做"别名稳定不变的直链"，可以考虑一个 `/r/:queryId` 的路由兜底，不走 queryCode。
- **F. 迁移脚本的归档**：`migrate-consolidate-outbox.mjs` 和 `migrate-inbox.mjs` 都是一次性任务。可以挪到 `scripts/_archived/` 或加个 README 明示"已过保质期"，避免未来新人误跑。

### 判断与决策

我倾向**只做 A + B，其他 defer**。理由：
- A 是文档债，一改了之，不会再回头
- B 是机械护栏，成本 5 分钟，收益是防止未来回踩
- C~F 都属于"想得到但当前没痛"，写进 MEMORY 就行，真痛起来再动

要不要我现在就动 A + B？如果你点头，我今天收尾顺手做了。

---

## A+B 两组 taskId 收尾（下午落地）

### 触发
上一节复盘后用户点头："立刻做吧。这次改动看起来小，但可能会有一些位置的影响——因为 taskId 之前已经存在各个逻辑里面了，这次修改，要保证修改到位，不要带来各种断联。"

### 全量扫描 taskId 引用点（先做影响分析）
把整个 repo（src / vite-plugins / scripts / tests / docs）扫了一遍，taskId 引用收敛在：
- **src/**：contract.ts（makeTaskId）/ dataSource.ts（URL 映射）/ store.ts（localStorage 键）/ contractAdapter.ts / EvaluationRunModal.tsx（WeakSet 去重）/ ReportPage.tsx（flattenTaskVersions + latestMtime 聚合）
- **vite-plugins/**：bus/handlers/inbox.ts（POST 合并）/ bus/handlers/outbox.ts（写入目录）/ bus/helpers.ts（parseQueryCode）/ codeRegistry.ts（parsePrefixCode / renamePrefix）
- **scripts/**：lint-outbox.mjs / bake-public-data.mjs / cleanup-orphan-inbox.mjs / migrate-consolidate-outbox.mjs / replace-report.mjs / migrate-inbox.mjs / seed-queries.mjs
- **tests/**：dataSource.test.ts / lintOutbox.test.ts
- **docs**：.evaluations/EVALUATION_CONTRACT.md（v3.4 变更节已落地）

**关键发现**：`tests/lintOutbox.test.ts` 里用 `taskId: "EV-0001-abcDEF"` + `contractVersion: "2.1"`——新 v3.4 约束不会误伤（只对 cv==="3.4" 激活），确认安全后才动手。

### A 组：清文档债（4 处，纯文案）
**A1 · EvaluationRunModal.tsx WeakSet 注释**：旧注释说"快速关闭会产生 nano6 孤儿"——这在扁平化后不成立（重复 POST 走后端合并语义，不会再生孤儿目录）。改成"虽不会生孤儿，但仍要避免无谓重复 POST（I/O、日志、用户噪音）"。

**A2 · scripts/replace-report.mjs CLI 示例**：`--task EV-0007-QeLgZE` → `--task EV-0007`，并加一行备注说明"v3.4 起 taskId === queryCode；历史 suffix 形态也兼容"。

**A3 · scripts/migrate-inbox.mjs CLI 帮助**：同样 `--task EV-0005-LlSiEs` → `--task EV-0005`，加同款兼容备注。

**A4 · tests/dataSource.test.ts URL 样本**：新增两组扁平化样本（`/_bus/outbox/EV-0002` → `/outbox/EV-0002/bundle.json`、`/_bus/outbox/EV-0005/v/3` → `/outbox/EV-0005/v3.json`），与历史 suffix 样本并存，确保新旧两种形态都在正则覆盖内。多位版本号（v10、v42）也在扁平化下验证一次。

### B 组：lint 硬约束（防未来回踩）
**B1 · validatePayload 签名微调 + v3.4 扁平化硬约束**：
- 给 `validatePayload(file, payload, errors)` 加可选第 4 参 `expectedTaskId`——来自 outbox 目录名。单测不传就只校验格式，兼容旧测试 API。
- 新规则仅在 `cv === "3.4"` 时激活：
  1. `taskId` 必须匹配 `^[A-Z]+-\d+$`（无 suffix）
  2. 若传了 `expectedTaskId`，`payload.taskId` 必须与之完全一致（防目录漂移）
- `lintOutbox()` 里从 `path.basename(path.dirname(file))` 推出目录名传入。
- 历史 v1.0 ~ v3.3 产物不受新约束影响（读兼容资产，保护不动）。

**lintOutbox.test.ts 配套单测（6 个新 case）**：
- 扁平化 taskId 合法 / 历史 suffix 形态被抓 / 目录名漂移被抓 / 一致时放行 / v3.3 兼容路径不激活新规则 / 不传 expectedTaskId 不误报目录错。

### 反例实测（beyond 单测）
写了个临时 `/tmp/verify-v34-taskid.mjs` 直接 import `validatePayload` 跑四条反例：
- 反例 1（v3.4+suffix）→ 1 条 taskId error："v3.4 要求扁平化 taskId..."
- 反例 2（v3.4+目录漂移）→ 1 条 taskId error："v3.4 要求 payload.taskId 必须等于 outbox 目录名..."
- 正例（v3.4 扁平化一致）→ 0 error
- 兼容（v3.3+suffix）→ 0 error

四条语义全部符合预期。临时脚本已清理。

### 回归四连（全绿）
- `npm run lint:outbox` ✅（16 outbox + 6 inbox，新约束对现有历史产物零误伤）
- `npm test` ✅（**134 pass**，此前 126 pass → 新增 dataSource 2 条 + lintOutbox 6 条 = +8，完全对得上）
- `npx tsc -p tsconfig.app.json --noEmit` ✅
- `npm run lint`（ESLint）✅

### 复盘：这次改动真的"没有断联"吗？
影响分析 → 动手前先扫全量引用点 → 特别 care 旧单测的 contractVersion（2.1 不会被新约束波及）→ 给 validatePayload 加第 4 参而不是改签名（单测 API 保持 backward-compat）→ 单测 + 反例双路验证 → 回归四连全绿。**A 组改的是文案，零语义风险；B 组改的是 lint 新增分支，旧路径一字未动**。断联面控制住了。

### 残留（后续 defer）
复盘里的 C~F（codeRegistry renamePrefix 注释 / outbox 目录锁 / queryId 作为备选 URL 段 / 一次性迁移脚本归档）全部未动，维持原判断——当前无痛，真痛了再说。

---

## EV-0002 评测产出 · v5（contractVersion 3.4）

### 产物路径
`.evaluations/outbox/EV-0002/v5.json`（v3.4 扁平化目录首条新写入）

### 排名与得分
| # | 产品 | 总分 | 评级 | veto |
|---|------|-----|------|------|
| 1 | MiroThink (`6q5vDnqkfk`) | 8.0 | 优秀 | 无 |
| 2 | **SophiaAI v5** (`SgBQUeWuiQ`) | 6.8 | 合格 | 无 |
| 3 | Manus (`Qm2DpyIJ-h`) | 6.3 | 合格 | 无 |
| 4 | SophiaAI v4 (`F6BEti3wFm`) | 5.4 | 待改进 | 无 |
| 5 | Gemini (`AhuI4bVzZS`) | 5.0 | 待改进 | 无 |

### 核验重点（外部核验共 7 组关键数字）
- **verified-correct**：2024/2025/2026Q1 首付款与占比、荣昌-艾伯维 56/6.5 亿、石药-阿斯利康 185/12 亿、礼新-默沙东 5.88/32.88=17.9%、亨利-诺和诺德 8.3 亿索赔、2020 批次 40% 退货率、Gemini 的 2026 至 2 月中旬 490 亿美元（医药魔方/163 口径）。
- **refuted（2 条）**：① SophiaAI v4「2024 首付同比-16.2%」与主流口径（2024=41 亿 vs 2023≈31.6 亿、同比 +30%）方向反向 —— 未触发 V1（量级一致、方向反）但 R1a 落 C；② SophiaAI v4 的「基于付费数据推导、误差±5%~±10%」挡箭牌无任何付费源名称/查询日期。
- **inconclusive（3 条）**：SophiaAI v5 的「1356.55×94.8%×(1-22%)」口径交叉；Manus 的「总额 × 退货率」基数放大；Gemini 的「1.5% 行业纠纷率」无出处。

### 关键 Sophia 优化方向（写进 crossProductInsights weakerThan）
1. **R1a 信源层级**：v5 的 29 条全是二手研报/门户文章转引（eastmoney/xueqiu/phirda/10jqka），vs MiroThink 46 条 URL 脚注（J.P. Morgan PDF/Reuters/Skadden/特拉华法院）。穿透到医药魔方/DealForma 一手付费源或 MNC 财报是 R1 从 B 冲 A 的关键。
2. **R3 三阶洞察**：v5 到二阶（MNC 专利悬崖驱动低首付+高里程碑），但没有 MiroThink 的『媒体按总额 / MNC 按里程碑——双方对交易成功的定义从签约那一刻就分歧』这种结构性观察。
3. **R5 元层级 caveat**：v5 的 1800 亿美元未兑现测算没给置信区间；MiroThink 主动披露『所有数字均为量级估算』『保密 ADR 使公开案例仅为冰山一角』。

### 回归
- `python3 -c "import json; json.load(...)"` ✅ JSON 合法
- `npm run lint:outbox` → `✅ lint OK: checked 17 outbox + 6 inbox files, no violations`（比之前 16 outbox 多了刚写入的 v5）

### 踩坑：长 JSON 分段写入
单次 write_to_file 输出 token 被 24000 限制截断，改策略为"先写骨架占位符 → 用多次 replace_in_file 分段 append"解决。未来写任何 >500 行的评测产物都走这个流程，避免重复出错。

