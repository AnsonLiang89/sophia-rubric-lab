# 2026-06-04 工作日志

## EV-0012 v4 评测（新增第 7 份候选 SophiaAI kkv4）

- 用户新增 Sophia 子版本分支 kkv4（reportId `Ol4nvvbaew`，8940 字），要求综合前 3 轮做横向校验、全面评估。产物 `outbox/EV-0012/v4.json`（contractVersion 3.7）。
- **kkv4 定档 R1~R5 全 A = 8.0 优秀，7 份中第 2（仅次 v10 的 8.7）**。最终排名：v10 8.7🏆 / **kkv4 8.0🆕** / Manus 7.8 / v5 7.2 / Kimi 6.9 / Miro 6.0 / Gemini 5.4。无 veto。
- kkv4 画像：短而全、信源最规范（28 篇文献 + 主动标⚠️单源估计/免责声明）、信息密度最高；弱在非共识锐度不及 v10（AI 渗透率/800V 沿用主流口径未证伪）。
- **kkv4 外验落锤**：
  - 铌价口径错（refuted, kk_3）：「铌铁 65Nb 约 432 元/kg」实为五氧化二铌价，铌铁实约 26-27 万元/吨，且同段「26 万元/吨」内部矛盾。**属旁支风险因子（成本影响 3-5%）非承重骨架，点名扣分不触 veto**。
  - 正向核验：中国全形态 31.98 亿/14.1 万吨（与 v10 同源互证）、全球 942.7M$/8.57 万吨/1.1 万$每吨（三数自洽）、日立 49.71%(2019)、800V 双口径区分均 verified-correct。
- **三 Sophia 分支梯队结论**：v10（非共识锐度+决策去泡沫化，R3/R5 双 S）> kkv4（完备性+信噪比，短而准）> v5（被 $2800/kg 带病推理拖累 R1=B）。kkv4 的并行方案成本区间(50-80元/kg)与行业实际一致，价格严谨性明显胜 v5。
- 老 6 份横向校验后**无新证据、维持原档**，仅更新 deltaReason。SBS 扩到 C(7,2)=21 对；factCoverageMatrix/queryCoverageMatrix 各加 kkv4 列；crossProductInsights 新增 v10-vs-kkv4 R3 对标 + 三 Sophia 分支 sharedWeakness（情景预测缺概率模型）。
- 回归：lint:outbox(31 outbox)✅ + npm test 149/149✅ + tsc✅ + build:public✅。

## 工程踩坑：bake dangling reportId（Submission/Inbox 平行链）

- 新增候选 kkv4 后 `build:public` 的 bake 报 `[bake:error] dangling reportId=Ol4nvvbaew`——因为 **Submission(localStorage 快照 `_runtime-snapshot.json`) 与 Inbox/Outbox(磁盘契约) 是两条平行存储链**，新增候选只进了 inbox/outbox，snapshot 的 submissions 缺对应记录。
- **修复方法**：往 `.evaluations/_runtime-snapshot.json` 的 `submissions` 数组补一条 `{id: <reportId>, queryId: <query.id>, productId: <PRODUCTS.json里的id>, content: <正文>, contentFormat:"markdown", submittedAt/createdAt}`。其中 `id` 必须 = reportId（bake 用 id 解析），`productId` 必须在 PRODUCTS.json 已注册（kkv4 对应 `sophia-kkv4`，本次已存在）。
- 补完重跑 build:public，dangling 消除，kkv4 正确进入 dist/public 的 EV-0012 v4 + bundle + public-bundle。
