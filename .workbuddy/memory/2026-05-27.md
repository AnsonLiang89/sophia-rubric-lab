# 2026-05-27 工作日志

## EV-0011 评测（Sophia 角色）
- 任务：换届前一年政策铁拳命题的辩证检验，3 candidates（SophiaAI v5 / Manus / Gemini）。
- 产出：`.evaluations/outbox/EV-0011/v1.json`，contractVersion=3.4，无历史版本（首次评测）。
- 总分：SophiaAI 8.0 / Manus 7.8 / Gemini 6.9（合格上限）；无 V1~V5 触发。
- 关键发现：Sophia 的核心短板是「三情景概率 65/25/10」自我标准不一致（自己审计否定其他子报告的伪精确，自己却在情景概率上重复同样毛病），以及未正面化解 Manus 的「反内卷=铁拳新形态」反向论证。Gemini R4 因为表格被压成长 plain text 降到 C 档。
- lint:outbox 通过。

## EV-0011 v2 复评
- 触发：用户要求按 v3.6 SOP 重新独立评测一轮。
- 产出：`.evaluations/outbox/EV-0011/v2.json`，独立重评后总分与 v1 完全一致（8.0 / 7.8 / 6.9），无维度跨档、无 veto 翻转——history-gate 闸门：事实集与 rubric 未变，独立结论与上版一致即合规。
- 新增 `overallScores[].deltaReason` 字段记录这一一致性结论。
- lint:outbox 通过（26 outbox + 10 inbox）。

## 踩坑（已记录在专家包外的工程经验里，下次直接复用）
- JSON 内 `comment` / `report` 字段里的 markdown 引用片段「...」如果含未转义的 ASCII `"`，会直接导致 JSON 解析失败。**写产物时所有 markdown 内的中文双引号一律用 “/”、‘/’ 或 『』 替代 ASCII `"`**，避免反复修。
- lint-outbox 期望的字段名是 `dimensionId` 而不是 `id`（rubric 与 extraDimensions 都是），契约 §3.0 顶层结构示例里写的是 `id` 但 lint 要的是 `dimensionId`，**这里有契约示例与 lint 实现不一致**。下次直接用 `dimensionId`。
- extraDimensions 即使 `activated=false` 也必须有 `scores: []` 字段（`必须是数组` 校验）。
