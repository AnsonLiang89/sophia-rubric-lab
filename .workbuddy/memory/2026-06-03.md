# 2026-06-03 工作日志

## EV-0012 评测（非晶纳米晶粉末产业深度研究横评）

- 任务：6 份候选（SophiaAI v10/v5、MiroThink、Manus、Gemini、Kimi），契约 v3.6，产物写入 `outbox/EV-0012/v1.json`。
- 数据异常：inbox 初版 Gemini 槽位与 Manus md5 逐字节相同 → 提示用户，用户重贴后 Gemini 为独立纯工艺导向报告，按 6 份独立评。
- 结果排名：SophiaAI v10=8.7 卓越🏆 / Manus=7.8 / SophiaAI v5=7.2 = Kimi=7.2 / MiroThink=6.0 合格 / Gemini=5.4 待改进。无 veto。
- 两处外部落锤核验：
  1) **Gemini「MPP 占全球金属粉芯需求 49% 以上」= refuted**（实测铁硅铝占 70%、MPP 约 20%，且与其"军工小众"自相矛盾）→ R1 降 C；属占比错非 V1-V5，不触发 veto。
  2) **SophiaAI v10「巴西铌产量 88%/中国<1%/进口依赖>95%」= verified-correct**（巴西储量 90%+、中国资源 0.8%、铌铁对外依存 96.9%）。
  3) v5 `$2800/kg` 基准价 = refuted（行业均价约 80 元/kg，偏差约 250 倍）；Kimi 多处无源拟真数字（iPhone16 Pro 2.5亿片/华为良率98.7%/GB300单台~5000电感）= inconclusive。
- SophiaAI v10 R3=S/R5=S 的核心依据：对"AI 电源渗透率 18%→35%"与"800V 全面爆发"两个最流行承重预测做了反向证伪。

## EV-0012 v2 复评（综合 v1 + 三关注点重审）

- 用户要求按三视角重做并综合 v1：①子问题是否正面回答 ②有效辅助决策 vs 堆字数 ③差异化/非共识观点。产物 `outbox/EV-0012/v2.json`（contractVersion 3.6）。
- **复用 v1 全部核验结论**（history-gate 精神）：铌正确/MPP证伪/v5价格250倍偏差/Kimi三处无源拟真，均未改判。
- **唯一有据档位修正**：Kimi R5 A→B（全场最长54907字却零caveat+无源拟真，"堆字数≠帮决策"），总分 7.2→6.9、优秀→合格。写了 deltaReason。涉及 Kimi 的 4 对 SBS 同步重算（v5 vs Kimi 由 tie→v5 slight 胜；Manus vs Kimi 由 slight→clear）。
- **克制原则**：一度想降 Gemini R2→C，重读全文确认它八问基本都答了（市场表1-3/玩家表4/国产化率/未来量均有），降档会是冤案，故维持 B。不为"显示有变化"乱改档。
- 正文重写：新增「八子问题×六产品正面回答矩阵(✅🔶❌)」「决策有效性vs篇幅对照表」「差异化非共识汇总表」三块，显式对应三关注点。
- 排名：v10=8.7🏆 / Manus=7.8 / v5=7.2 / **Kimi=6.9(降档)** / Miro=6.0 / Gemini=5.4。
- 回归：lint:outbox ✅(29 outbox)、完整3.6规则自查全过（加权和精确/coverage 100%/SBS 15对/四段锚点齐）。

## 契约升级 v3.7 落地（已完成全链路回归）

- 基于 EV-0012 复盘的 5 条建议，落地 P0+P1（建议1/2/3/5），暂缓建议4(信息密度扩展维度,触权重)。
- **改动文件**：
  1. `EVALUATION_CONTRACT.md`：升 3.7；新增 §3.13 queryCoverageMatrix；§1 硬约束加 19/20；§3.0/§3.1/§5.1 SOP/§5.4 自检/§7 版本历史同步。
  2. `RUBRIC_STANDARD.md`：升 3.7；R2 checklist#1 强化(复合query拆子问题)；R3 checklist#3 加跨产品非共识归集；R5 checklist 5→6项(新增信噪比负向)+双轴表纵轴加信噪比+反例；附录版本表。
  3. `scripts/lint-outbox.mjs`：**又发现两处枚举门坑**——`isV36=cv==="3.6"` 和 `contractVersion 白名单` 和 `v3.4扁平化门` 都是精确枚举，3.7 会漏。统一改 `cvNum>=` 语义(isV36Plus/isV37Plus/isClaimTopTen/cvNumEarly>=3.4)。新增 queryCoverageMatrix 校验(子问题≥2必填+覆盖全candidates+enum+partial/missing必给note) 和 R5 checklist≥6(仅3.7+)。
  4. `src/lib/contract.ts`：contractVersion union 加 "3.7"；新增 QueryCoverageMatrix 等类型。
  5. `src/lib/contractAdapter.ts`：DEFAULT_RUBRIC_NAMES 补 "3.7"(TS Record 强制)。
  6. `tests/lintOutbox.test.ts`：+10 个 3.7 单测(queryCoverageMatrix 6条 + R5≥6 3条 + 向后兼容)，139→149。
- **示范产物**：EV-0012 v3.json(cv 3.7)：把 v2 正文的子问题矩阵升格为结构化 queryCoverageMatrix(8子问题×6产品)，R5 加第6条信噪比item，全产品带 deltaReason(说明本轮=结构升级、分数延续v2无新证据)。排名不变。
- **回归全绿**：lint:outbox(30 outbox)✅ + npm test 149/149✅ + tsc✅ + build:public✅。
- **教训沉淀**：lint 里凡按 contractVersion 分支的判断，一律用 `cvNum>=` 数值语义，禁用精确枚举/枚举白名单——这是第三次踩同一个坑(3.6门/contractVersion白名单/3.4扁平化门)。

## EV-0012 两轮评测复盘 + Rubric 优化建议（已成文，待 Anson 拍板）

- 产物：`.evaluations/EV-0012_评测复盘与rubric优化建议.md`。性质=方法论复盘+建议，**未改任何契约/标准基线**。
- 核心发现：两轮唯一差异是 Kimi R5 A→B（7.2→6.9），**不是新证据、是新视角**（v1 rubric 没强制看"信噪比/篇幅膨胀"）。暴露 rubric 三短板：
  - **短板A**：R2 对多子问题 query 缺"子问题×产品"强制矩阵，"八问全覆盖=A"可偷懒，区分度被压平。
  - **短板B**：R5 缺"信息密度/信噪比"负向锚点，checklist 全是正向加分项，默认"内容多=价值高"踩反；R4(完备)与R5(决策价值)在长报告上其实反向。
  - **短板C**：R3 非共识是单产品 yes/no，无跨产品归集，"谁的非共识更值钱"无结构化落点。
- 硬证据：全场最短 Manus(12678字)决策价值(A)>全场最长 Kimi(54907字)(B)。
- 优化建议（P0→P2）：①新增 queryCoverageMatrix(子问题×产品✅🔶❌，lint可校验) ②R5加信噪比负向checklist+反例 ③R3跨产品非共识归集表 ④info-mining默认建议激活"信息密度"扩展维度(暂缓,触权重) ⑤history-gate补"差异源=新证据还是新视角"自检。
- **明确不动**：权重40/15/20/10/15、5档制、V1~V5 veto、factCoverageMatrix（与新增queryCoverageMatrix正交：一个管事实对不对，一个管子问题答没答）。
- 落地若采纳→升 contractVersion 3.7，旧产物走 §6.2+legacy grandfather 不迁移。

## lint-outbox.mjs 版本门修复（已完成）

- **根因**：`isV2Plus/isV22Plus/isV3` 三个布尔门使用枚举白名单，只列到 3.3。3.4/3.5/3.6 产物的核心校验（档位制/veto/加权和/claim/证据密度）全部被跳过。
- **修复**（3 处）：
  1. `isV2Plus/isV22Plus/isV3` 改为 `cvNum >= 2.0 / 2.2 / 3.0` 语义比较，杜绝未来新增版本时再漏。
  2. `targetMinutes` 门：`isV33 = cv==="3.3"` → `cvNum >= 3.3`，3.3+ 仅要求正数、不锁 45。
  3. 四段正文锚点门：`isV31||isV32||isV33` → `cvNum >= 3.1`，3.1~3.6+ 共享。
- **历史产物处理**：修复后暴露 53 条真实违规（14 个历史文件）。按 contract §6.2 不可回改已发布结论，新增 `LEGACY_GRANDFATHER` 集合对 EV-0002/v5、EV-0003/v1、EV-0004/v2v3、EV-0006/v1、EV-0007/v1v2v3、EV-0008/v1、EV-0009/v1、EV-0010/v1、EV-0011/v1v2v3 共 14 个文件的违规降级为 warning（打印但不阻断 exit code）。EV-0012 及所有未来产物走全严格校验。
- **回归结果**：`lint:outbox` ✅ + `npm test` 139/139 ✅ + `tsc --noEmit` ✅。
