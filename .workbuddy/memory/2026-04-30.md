# 2026-04-30

## EV-0007 评测报告 PDF + 腾讯文档
- 从 `.evaluations/outbox/EV-0007/v3.json` 提取报告正文
- 用 reportlab 生成精美 PDF：`EV-0007-Report.pdf`（310KB）
- 创建腾讯文档在线智能文档：https://docs.qq.com/aio/DRFVMRUt2Q1N0Z3JY
- file_id: DULEKvCStgrX
- 权限已设为"所有人可查看"（policy=2），供外部人员访问

## SPA 路由修复（对外版子路由直接访问）
- **根因**：`index.html` 恢复脚本里 `l.search.slice(2)` 把 `/` 也去掉了，导致拼接出 `/sophia-rubric-labstandard` 这种无效路径
- **修复**：改为 `.slice(1)` 保留开头的 `/`，base 用 `pathname.replace(/\/$/, "")` 去尾斜杠
- 修复后路径恢复：`/sophia-rubric-lab` + `/standard` = `/sophia-rubric-lab/standard` ✅
- 已 push 触发 GitHub Actions 部署，约 1-2 分钟后生效
